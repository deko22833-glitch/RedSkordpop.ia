# 📤 Загрузка Redskord на GitHub

## Быстрый способ (автоматический)

1. **Запустите скрипт:**
   ```
   deploy-to-github.bat
   ```

2. **Следуйте инструкциям:**
   - Скрипт автоматически инициализирует Git
   - Добавит все файлы
   - Создаст коммит
   - Попросит URL вашего репозитория

3. **Создайте репозиторий на GitHub:**
   - Перейдите: https://github.com/new
   - Название: `Redskord` (или любое другое)
   - **НЕ** добавляйте README, .gitignore или лицензию
   - Нажмите "Create repository"

4. **Скопируйте URL репозитория** (например):
   ```
   https://github.com/deko22833-glitch/Redskord.git
   ```

5. **Вставьте URL в скрипт** и нажмите Enter

6. **Готово!** Проект загружен на GitHub

## Ручной способ

### Шаг 1: Установите Git

Если Git не установлен:
- Скачайте: https://git-scm.com/download/win
- Установите с настройками по умолчанию

### Шаг 2: Создайте репозиторий на GitHub

1. Перейдите: https://github.com/new
2. Название: `Redskord`
3. Описание (опционально): "Redskord Messenger - локальный мессенджер"
4. **Public** или **Private** (на ваше усмотрение)
5. **НЕ** добавляйте README, .gitignore или лицензию
6. Нажмите "Create repository"

### Шаг 3: Инициализируйте Git в проекте

Откройте PowerShell или CMD в папке проекта:

```bash
cd "C:\Новая папка"

# Инициализация
git init
git branch -M main

# Настройка пользователя (если еще не настроено)
git config --global user.name "deko22833-glitch"
git config --global user.email "ваш@email.com"

# Добавление файлов
git add .

# Создание коммита
git commit -m "Initial commit: Redskord Messenger"

# Подключение к GitHub
git remote add origin https://github.com/deko22833-glitch/Redskord.git

# Отправка на GitHub
git push -u origin main
```

### Шаг 4: Проверка

Откройте ваш репозиторий:
```
https://github.com/deko22833-glitch/Redskord
```

Вы должны увидеть все файлы проекта!

## 🔐 Авторизация в Git

Если при `git push` запрашивается авторизация:

### Способ 1: Personal Access Token (рекомендуется)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → выберите `repo` (полный доступ к репозиториям)
3. Скопируйте токен
4. При `git push` используйте токен вместо пароля

### Способ 2: GitHub Desktop

1. Скачайте: https://desktop.github.com/
2. Войдите в аккаунт
3. File → Add Local Repository → выберите папку проекта
4. Publish repository → выберите аккаунт и название

## ☁️ После загрузки на GitHub

Теперь можете деплоить на бесплатный хостинг:

### Render (рекомендуется)

1. Перейдите: https://render.com
2. Sign up → подключите GitHub аккаунт
3. New → Web Service
4. Выберите репозиторий `Redskord`
5. Настройки:
   - **Name:** redskord
   - **Environment:** Node
   - **Build Command:** `npm install --only=production`
   - **Start Command:** `node server/index.js`
   - **Plan:** Free
6. Create Web Service
7. Дождитесь деплоя (2-3 минуты)
8. Получите адрес вида: `https://redskord.onrender.com`

### Railway

1. Перейдите: https://railway.app
2. Sign up → подключите GitHub
3. New Project → Deploy from GitHub repo
4. Выберите `Redskord`
5. Railway автоматически определит настройки
6. Получите адрес вида: `https://redskord.up.railway.app`

## ✅ Готово!

После деплоя ваш Redskord будет доступен по HTTPS адресу 24/7 без вашего ПК!

