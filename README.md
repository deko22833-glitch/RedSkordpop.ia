# 🔴 Redskord Messenger

Мессенджер в стиле Discord с чатами, видеозвонками, системой друзей и личными сообщениями. Ваш компьютер работает как сервер.

## ✨ Новые возможности

- ✅ **Регистрация и авторизация** - создайте свой аккаунт
- ✅ **Система друзей** - добавляйте друзей и общайтесь
- ✅ **Личные сообщения** - приватные чаты с друзьями
- ✅ **Discord-подобный дизайн** - темная тема в стиле Discord
- ✅ **Общий чат** - общайтесь со всеми пользователями
- ✅ **Видеозвонки** - WebRTC звонки с друзьями
- ✅ **Доступ по сети** - другие пользователи могут подключиться

## 🚀 Установка и запуск

### 1. Установите Node.js
Скачайте и установите Node.js с [nodejs.org](https://nodejs.org/) (версия 16 или выше)

### 2. Установите зависимости
```bash
npm install
```

### 3. Запуск сервера

**Вариант 1: Через .bat файл (рекомендуется)**
- Дважды кликните на `start-server.bat`
- Сервер запустится автоматически

**Вариант 2: Через командную строку**
```bash
npm start
```

### 4. Откройте мессенджер
- Откройте браузер и перейдите на `http://localhost:3000`
- Или используйте Electron приложение (см. ниже)

### 5. Подключение других пользователей

**Локальная сеть:**
Когда сервер запущен, он покажет сетевой адрес (например, `http://192.168.1.100:3000`).
Другие пользователи в вашей сети могут открыть этот адрес в браузере и подключиться.

**Доступ из интернета (для разных провайдеров):**
Сервер автоматически определит ваш публичный IP адрес. Чтобы пользователи разных провайдеров (Билайн, МТС, Теле2 и др.) могли подключаться:

1. **Настройте Port Forwarding на роутере:**
   - Откройте настройки роутера (обычно `192.168.1.1`)
   - Найдите "Port Forwarding" / "Проброс портов"
   - Добавьте правило для порта 3000 (TCP)
   - Укажите внутренний IP вашего компьютера

2. **Откройте порт в файрволе Windows:**
   - Брандмауэр → Правила для входящих → Создать правило
   - Порт 3000 (TCP) → Разрешить

3. **Поделитесь адресом:**
   - Сервер покажет публичный IP при запуске
   - Друзья подключаются по адресу: `http://ВАШ_IP:3000`

📖 **Подробная инструкция:** См. файл `setup-external-access.md`

**Альтернатива (ngrok):**
Для быстрого тестирования можно использовать ngrok:
- Запустите `start-with-ngrok.bat`
- Скопируйте предоставленный адрес и поделитесь с друзьями

**Для мобильных провайдеров (Билайн, МТС, Теле2):**
- **WebRTC звонки:** Автоматически работают через TURN серверы (настроено)
- **Подключение к серверу:** Если прямого подключения нет, используйте:
  - `start-with-cloudflare.bat` - создает постоянный адрес (рекомендуется)
  - `start-with-ngrok.bat` - быстрый способ для тестирования
  - Подробнее: `setup-external-access.md`

## 📦 Сборка .exe файла

Для создания исполняемого .exe файла:

```bash
npm run build-win
```

Готовый .exe файл будет в папке `dist/`

## 📤 Загрузка на GitHub

**Быстрый способ:**
1. Запустите `deploy-to-github.bat`
2. Следуйте инструкциям на экране
3. Создайте репозиторий на https://github.com/new
4. Вставьте URL репозитория в скрипт

📖 **Подробная инструкция:** См. файл `GITHUB_DEPLOY.md`

## ☁️ Деплой на хостинг (без вашего ПК)

### Render (просто и бесплатно)
1. Запушьте проект в GitHub
2. На `render.com` → New → Web Service → выберите репозиторий
3. Настройки:
   - Runtime: Node
   - Build command: `npm install --only=production`
   - Start command: `node server/index.js`
   - Environment: Node 18+
4. Нажмите Deploy — получите HTTPS адрес вида `https://redskord.onrender.com`

В проект добавлен `render.yaml` — можно использовать автонастройку.

### Railway (альтернатива)
1. Установите CLI: `npm i -g @railway/cli`
2. В папке проекта:
   - `railway login`
   - `railway init`
   - `railway up`

В проект добавлен `Procfile`.

### Docker (любой VPS/хостинг c контейнерами)
1. Собрать и запустить:
```bash
docker build -t redskord .
docker run -e PORT=3000 -p 3000:3000 redskord
```

Сервер читает порт из переменной окружения `PORT`.

## 🎯 Использование

### Регистрация
1. При первом запуске нажмите "Регистрация"
2. Введите имя пользователя и пароль
3. Email необязателен
4. Нажмите "Зарегистрироваться"

### Добавление друзей
1. Нажмите кнопку с иконкой пользователя в заголовке чата
2. Введите имя пользователя в поиске
3. Нажмите "Добавить" рядом с пользователем
4. Пользователь получит запрос в друзья
5. После принятия запроса вы сможете писать друг другу

### Личные сообщения
1. В левой панели найдите друга в разделе "ЛИЧНЫЕ СООБЩЕНИЯ"
2. Кликните на друга, чтобы открыть личный чат
3. Начните общение!

### Видеозвонки
1. Откройте личный чат с другом
2. Нажмите кнопку звонка в заголовке
3. Примите разрешения на камеру и микрофон
4. Друг получит уведомление о входящем звонке

## 📁 Структура данных

Все данные хранятся в папке `data/`:
- `users.json` - информация о пользователях
- `messages.json` - личные сообщения

## 🔧 Технологии

- **Backend**: Node.js, Express, Socket.io
- **Frontend**: HTML, CSS, JavaScript
- **WebRTC**: Для видеозвонков
- **Electron**: Для создания .exe приложения
- **База данных**: JSON файлы (простое хранилище)

## ⚠️ Важно

- Сервер должен быть запущен для работы чата и звонков
- Для видеозвонков нужна камера и микрофон
- Другие пользователи могут подключиться по IP адресу вашего компьютера (показывается при запуске сервера)
- Убедитесь, что порт 3000 не заблокирован файрволом
- Все данные хранятся локально на вашем компьютере

## 🎨 Особенности дизайна

- Темная тема в стиле Discord
- Три панели: серверы, каналы/друзья, чат, пользователи
- Интуитивный интерфейс
- Адаптивный дизайн

## 🔒 Безопасность

- Пароли хранятся в виде хешей SHA-256
- Данные хранятся локально
- Нет подключения к внешним серверам

## 📝 Примечания

- Это локальный мессенджер для использования в локальной сети
- Для работы в интернете потребуется настройка портов и возможно домен/IP
